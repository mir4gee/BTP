# Pinned to versions used by the official RiemannGFM repo.
# For local Docker dev we swap the CUDA torch wheel for the CPU wheel — the
# public torch==2.0.0 wheel on PyPI is CPU-only, so plain `torch==2.0.0`
# resolves correctly inside the image.
torch==2.0.0
torch_geometric==2.6.1

# torch_scatter must match the torch build. In the CPU Docker image we install
# it from the cpu wheel index; on Colab the notebook install cell pulls the
# cu118 build. Leaving it unpinned here so `pip install -r requirements.txt`
# on Colab picks up the right wheel via the extra-index-url passed at install
# time.
torch_scatter

geoopt==0.5.0
gensim==4.3.3
matplotlib==3.7.1
networkx==3.0
numpy==1.24.2
ogb==1.3.6
scikit-learn==1.3.2
scipy==1.10.1
tqdm==4.61.2

# Dev-only extras — safe to keep in the same file since Docker image is dev-oriented.
pytest==8.2.0
pyyaml==6.0.1
